.search
    .search-input
        input(type="text", placeholder="search everywhere", (keyup)="actionSearch($event.target.value)") 
        .clearfix
    .search-view
        div(*ngFor="let post of posts")
            div="{{post.title}}"
            div="{{post.label}}"
            div="{{post.date}}"
            div="{{post.image}}"
            div="{{post.content}}"
            div
                a((click)="actionNavigate(post)") this is the link
            br